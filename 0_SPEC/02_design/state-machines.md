# 상태 머신
## 업무 개선 보드 시스템

---

## 1. 개선 항목 상태 머신

### 1.1 상태 정의

| 상태 | 코드 | 아이콘 | 색상 | 설명 |
|------|------|--------|------|------|
| 떠올림 | IDEA | 💡 | #FCD34D (노랑) | 아이디어 단계 |
| 보고 있음 | REVIEWING | 👀 | #60A5FA (파랑) | 검토 중 |
| 만지는 중 | IN_PROGRESS | 🛠️ | #34D399 (초록) | 진행 중 |
| 잠깐 멈춤 | ON_HOLD | ⏸️ | #9CA3AF (회색) | 보류 |
| 정리됨 | DONE | ✅ | #2DD4BF (민트) | 완료 |

### 1.2 상태 전이 다이어그램

```
                    ┌─────────────────────────────────┐
                    │                                 │
                    ▼                                 │
    ┌─────────┐  ──────▶  ┌─────────────┐  ──────▶  ┌──────────────┐
    │   💡    │           │     👀      │           │     🛠️       │
    │ 떠올림  │  ◀──────  │ 보고 있음   │  ◀──────  │ 만지는 중    │
    │  IDEA   │           │  REVIEWING  │           │ IN_PROGRESS  │
    └────┬────┘           └──────┬──────┘           └──────┬───────┘
         │                       │                         │
         │                       │                         │
         │                       ▼                         │
         │                ┌─────────────┐                  │
         │  ──────────▶   │     ⏸️      │  ◀──────────────┤
         │                │ 잠깐 멈춤   │                  │
         │                │  ON_HOLD    │                  │
         │                └──────┬──────┘                  │
         │                       │                         │
         │                       │                         │
         ▼                       ▼                         ▼
    ┌─────────────────────────────────────────────────────────┐
    │                          ✅                              │
    │                       정리됨                             │
    │                        DONE                              │
    └─────────────────────────────────────────────────────────┘
```

### 1.3 허용된 상태 전이

| 현재 상태 | 전이 가능 상태 |
|-----------|----------------|
| IDEA | REVIEWING, IN_PROGRESS, ON_HOLD, DONE |
| REVIEWING | IDEA, IN_PROGRESS, ON_HOLD, DONE |
| IN_PROGRESS | REVIEWING, ON_HOLD, DONE |
| ON_HOLD | IDEA, REVIEWING, IN_PROGRESS, DONE |
| DONE | IDEA, REVIEWING, IN_PROGRESS, ON_HOLD |

> 📌 모든 상태에서 다른 모든 상태로 전이 가능 (유연한 운영)

### 1.4 상태 전이 코드

```typescript
// types/status.ts
export enum ItemStatus {
  IDEA = 'IDEA',
  REVIEWING = 'REVIEWING',
  IN_PROGRESS = 'IN_PROGRESS',
  ON_HOLD = 'ON_HOLD',
  DONE = 'DONE'
}

export const STATUS_CONFIG = {
  IDEA: {
    label: '떠올림',
    icon: '💡',
    color: '#FCD34D',
    bgColor: '#FFFBEB'
  },
  REVIEWING: {
    label: '보고 있음',
    icon: '👀',
    color: '#60A5FA',
    bgColor: '#EFF6FF'
  },
  IN_PROGRESS: {
    label: '만지는 중',
    icon: '🛠️',
    color: '#34D399',
    bgColor: '#ECFDF5'
  },
  ON_HOLD: {
    label: '잠깐 멈춤',
    icon: '⏸️',
    color: '#9CA3AF',
    bgColor: '#F3F4F6'
  },
  DONE: {
    label: '정리됨',
    icon: '✅',
    color: '#2DD4BF',
    bgColor: '#F0FDFA'
  }
};

// 상태 전이 유효성 검사 (모든 전이 허용)
export const isValidTransition = (
  from: ItemStatus,
  to: ItemStatus
): boolean => {
  // 같은 상태로 전이는 불가
  return from !== to;
};
```

---

## 2. 사용자 인증 상태 머신

### 2.1 상태 정의

| 상태 | 설명 |
|------|------|
| LOGGED_OUT | 로그아웃 상태 |
| LOGGING_IN | 로그인 시도 중 |
| LOGGED_IN | 로그인 완료 |
| TOKEN_EXPIRED | 토큰 만료 |
| REFRESHING | 토큰 갱신 중 |

### 2.2 상태 전이 다이어그램

```
    ┌──────────────┐
    │  LOGGED_OUT  │◀─────────────────────────────────┐
    └──────┬───────┘                                  │
           │                                          │
           │ 로그인 시도                               │ 로그아웃
           ▼                                          │
    ┌──────────────┐                                  │
    │  LOGGING_IN  │                                  │
    └──────┬───────┘                                  │
           │                                          │
           │ 로그인 성공                               │
           ▼                                          │
    ┌──────────────┐     토큰 만료     ┌──────────────┐
    │  LOGGED_IN   │ ───────────────▶  │TOKEN_EXPIRED │
    └──────────────┘                   └──────┬───────┘
           ▲                                  │
           │                                  │ 갱신 시도
           │                                  ▼
           │                           ┌──────────────┐
           │         갱신 성공         │  REFRESHING  │
           └───────────────────────────└──────────────┘
                                              │
                                              │ 갱신 실패
                                              ▼
                                       [LOGGED_OUT]
```

---

## 3. 상태 변경 이력 관리

### 3.1 이력 기록 규칙

| 항목 | 규칙 |
|------|------|
| 기록 시점 | 상태 변경 시 자동 |
| 기록 항목 | 이전 상태, 새 상태, 변경자, 변경 시간, 메모 |
| 보존 기간 | 영구 |

### 3.2 이력 기록 코드

```typescript
// services/statusHistory.service.ts
export const recordStatusChange = async (
  itemId: string,
  fromStatus: ItemStatus | null,
  toStatus: ItemStatus,
  changedBy: string,
  note?: string
) => {
  return prisma.statusHistory.create({
    data: {
      itemId,
      fromStatus,
      toStatus,
      changedBy,
      note
    }
  });
};
```

---

## 4. 프론트엔드 상태 관리

### 4.1 Zustand 스토어

```typescript
// stores/itemStore.ts
import { create } from 'zustand';

interface ItemState {
  items: Item[];
  selectedItem: Item | null;
  filter: {
    departmentId: string | null;
    status: ItemStatus | null;
  };
  
  setItems: (items: Item[]) => void;
  selectItem: (item: Item | null) => void;
  updateItemStatus: (id: string, status: ItemStatus) => void;
  setFilter: (filter: Partial<ItemState['filter']>) => void;
}

export const useItemStore = create<ItemState>((set) => ({
  items: [],
  selectedItem: null,
  filter: {
    departmentId: null,
    status: null
  },
  
  setItems: (items) => set({ items }),
  selectItem: (item) => set({ selectedItem: item }),
  updateItemStatus: (id, status) => set((state) => ({
    items: state.items.map((item) =>
      item.id === id ? { ...item, status } : item
    )
  })),
  setFilter: (filter) => set((state) => ({
    filter: { ...state.filter, ...filter }
  }))
}));
```

---

**문서 버전**: 1.0  
**작성일**: 2026-01-19
