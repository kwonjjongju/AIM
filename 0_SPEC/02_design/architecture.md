# 시스템 아키텍처
## AIM (Automated Improvement Management)

---

## 1. 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         클라이언트 계층                          │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Desktop   │  │   Mobile    │  │   Tablet    │             │
│  │   Browser   │  │   Browser   │  │   Browser   │             │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘             │
└─────────┼────────────────┼────────────────┼────────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │ HTTPS
┌──────────────────────────┼─────────────────────────────────────┐
│                     프론트엔드 계층                              │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    React SPA                           │    │
│  │  ┌─────────┐ ┌──────────┐ ┌────────┐ ┌────────┐      │    │
│  │  │  Auth   │ │Dashboard │ │ Board  │ │ Admin  │      │    │
│  │  │ Module  │ │  Module  │ │ Module │ │ Module │      │    │
│  │  └─────────┘ └──────────┘ └────────┘ └────────┘      │    │
│  └────────────────────────────────────────────────────────┘    │
└──────────────────────────┬─────────────────────────────────────┘
                           │ REST API
┌──────────────────────────┼─────────────────────────────────────┐
│                      백엔드 계층                                 │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                  Node.js + Express                     │    │
│  │  ┌─────────┐ ┌──────────┐ ┌────────┐ ┌────────┐      │    │
│  │  │  Auth   │ │   Item   │ │  User  │ │  Dept  │      │    │
│  │  │Controller│ │Controller│ │Controller│ │Controller│    │    │
│  │  └────┬────┘ └────┬─────┘ └────┬───┘ └────┬───┘      │    │
│  │       └───────────┴────────────┴──────────┘           │    │
│  │                        │                              │    │
│  │  ┌────────────────────────────────────────────────┐   │    │
│  │  │              Service Layer                     │   │    │
│  │  └────────────────────┬───────────────────────────┘   │    │
│  └───────────────────────┼───────────────────────────────┘    │
└──────────────────────────┼─────────────────────────────────────┘
                           │
┌──────────────────────────┼─────────────────────────────────────┐
│                      데이터 계층                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   SQLite     │  │    Redis     │  │ File Storage │         │
│  │  (개발 DB)   │  │(Cache/Session)│  │ (Attachments)│         │
│  │ PostgreSQL   │  │              │  │              │         │
│  │  (운영 DB)   │  │              │  │              │         │
│  └──────────────┘  └──────────────┘  └──────────────┘         │
└────────────────────────────────────────────────────────────────┘
```

---

## 2. UI 아키텍처 (2026-01-19 업데이트)

### 2.1 레이아웃 구조

```
┌────────────────────────────────────────────────────────────────┐
│                    전체 레이아웃 (flex)                         │
├────┬────────┬──────────────────────────────────────────────────┤
│    │        │                   Header (h-14)                  │
│Icon│Submenu │  AIM System Title            User Info           │
│Bar │Sidebar ├──────────────────────────────────────────────────┤
│w-16│ w-220  │                                                  │
│    │        │                                                  │
│ 🏠 │ 전체   │                                                  │
│대시│ 💡아이디어│             Main Content (flex-1)              │
│보드│ 👀검토중 │                                                  │
│────│ 🛠️진행중 │               <Outlet />                        │
│ 📋 │ ⏸️보류   │                                                  │
│개선│ ✅완료   │                                                  │
│보드│        │                                                  │
│────│        │                                                  │
│ ⚙️ │ 부서   │                                                  │
│관리│ 사용자 │                                                  │
│────│        │                                                  │
│로그│        │                                                  │
│아웃│        │                                                  │
└────┴────────┴──────────────────────────────────────────────────┘
```

### 2.2 컴포넌트 계층

```
<Layout>
├── <IconBar>              // 좌측 아이콘 바 (w-16, bg-slate-700)
│   ├── <Logo>             // 시스템 로고
│   ├── <MenuIcons>        // 대시보드, 개선보드, 관리
│   └── <LogoutButton>     // 로그아웃
│
├── <SubmenuSidebar>       // 서브메뉴 사이드바 (w-220, AnimatePresence)
│   ├── <SidebarHeader>    // 메뉴 타이틀
│   └── <SubMenuItems>     // 하위 메뉴 항목들
│
└── <MainArea>             // 메인 영역 (flex-1)
    ├── <Header>           // 상단 헤더 (h-14)
    │   ├── <SystemTitle>  // AIM 타이틀
    │   └── <UserInfo>     // 부서 + 사용자명
    │
    └── <ContentArea>      // 콘텐츠 영역
        └── <Outlet />     // React Router Outlet
```

### 2.3 색상 테마

| 요소 | 색상 | Tailwind Class |
|------|------|----------------|
| 아이콘 바 배경 | #334155 | bg-slate-700 |
| 활성 메뉴 | #14B8A6 | bg-teal-500 |
| 서브메뉴 배경 | #F8FAFC | bg-slate-50 |
| 헤더 배경 | #FFFFFF | bg-white |
| 콘텐츠 배경 | #F1F5F9 | bg-slate-100 |
| 텍스트 (메인) | #1E293B | text-slate-800 |
| 텍스트 (서브) | #64748B | text-slate-500 |

---

## 3. 컴포넌트 설계

### 3.1 프론트엔드 컴포넌트

| 모듈 | 설명 | 주요 컴포넌트 |
|------|------|---------------|
| Layout | 레이아웃 | Layout (사이드바+헤더+콘텐츠) |
| Auth | 인증 관련 | LoginPage, AuthGuard |
| Dashboard | 대시보드 | DashboardPage, StatsSummary, DeptChart |
| Board | 보드/카드 | BoardPage, ImprovementCard, CreateItemModal |
| Admin | 관리자 | AdminPage, UserManagement, DeptManagement |
| Common | 공통 | StatusBadge, Modal, ItemDetailModal |

### 3.2 백엔드 컴포넌트

| 레이어 | 역할 | 주요 파일 |
|--------|------|-----------|
| Routes | 라우팅 | auth.routes.ts, item.routes.ts, user.routes.ts |
| Services | 비즈니스 로직 | auth.service.ts, item.service.ts, dashboard.service.ts |
| Middleware | 공통 처리 | auth.middleware.ts, error.middleware.ts |
| Config | 설정 | database.ts, jwt.ts |

---

## 4. 데이터 흐름

### 4.1 항목 등록 흐름

```
[사용자] 
    │
    │ 1. 등록 폼 입력
    ▼
[React: CreateItemModal]
    │
    │ 2. POST /api/v1/items
    ▼
[Express: item.routes.ts]
    │
    │ 3. 유효성 검증
    ▼
[item.service.ts]
    │
    │ 4. 비즈니스 로직
    ▼
[Prisma Client]
    │
    │ 5. INSERT INTO ImprovementItem
    ▼
[SQLite / PostgreSQL]
    │
    │ 6. 결과 반환
    ▼
[사용자: 등록 완료]
```

### 4.2 인증 흐름

```
[로그인 요청]
    │
    ▼
[auth.routes.ts]
    │
    ├─→ [auth.service.ts.login]
    │       │
    │       ▼
    │   [비밀번호 검증 (bcrypt)]
    │
    ├─→ [JWT 토큰 생성]
    │       │
    │       ├─ Access Token (1h)
    │       └─ Refresh Token (7d)
    │
    └─→ [응답: tokens + user]
```

---

## 5. 네트워크 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                        Internet                              │
└─────────────────────────┬───────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────────┐
│                     Nginx (Reverse Proxy)                    │
│                     - SSL Termination                        │
│                     - Static Files                           │
│                     - Load Balancing                         │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          │               │               │
          ▼               ▼               ▼
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  Frontend   │   │   Backend   │   │   Backend   │
│   (Vite)    │   │  (Node.js)  │   │  (Node.js)  │
│   :5173     │   │    :4000    │   │    :4001    │
└─────────────┘   └──────┬──────┘   └──────┬──────┘
                         │                 │
                         └────────┬────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
              ┌──────────┐ ┌──────────┐ ┌──────────┐
              │  SQLite  │ │  Redis   │ │  Files   │
              │ (개발)   │ │  :6379   │ │ Storage  │
              │PostgreSQL│ │          │ │          │
              │ (운영)   │ │          │ │          │
              └──────────┘ └──────────┘ └──────────┘
```

---

## 6. 보안 아키텍처

```
[클라이언트]
    │
    │ HTTPS (TLS 1.3)
    ▼
[Nginx]
    │
    │ SSL Termination
    ▼
[Backend API]
    │
    ├─→ [JWT 검증] ─→ 인증 실패 → 401
    │
    ├─→ [RBAC 검사] ─→ 권한 없음 → 403
    │
    ├─→ [입력 검증] ─→ 유효하지 않음 → 400
    │
    └─→ [비즈니스 로직 실행]
```

---

## 7. 확장성 고려사항

### 7.1 수평 확장
- Backend: 무상태 설계, 다중 인스턴스 지원
- Session: Redis 사용으로 세션 공유
- Database: Read Replica 지원 (향후)

### 7.2 캐싱 전략
| 대상 | 캐시 위치 | TTL |
|------|-----------|-----|
| 부서 목록 | Redis | 1일 |
| 사용자 정보 | Redis | 1시간 |
| 대시보드 통계 | Redis | 5분 |

---

**문서 버전**: 2.0  
**작성일**: 2026-01-19  
**변경 이력**:
- v1.0 (2026-01-19): 초기 작성
- v2.0 (2026-01-19): UI 아키텍처 섹션 추가 (좌측 사이드바 레이아웃)
